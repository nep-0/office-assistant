# Local HTTP Default With Documented HTTPS

The default local demo deployment serves HTTP for localhost use, while documentation explains how HTTPS or trusted certificates can be configured for real LAN deployment through Caddy. Certificate management is not part of the core final-year project implementation.

