# SQLite FTS For Management And Hybrid Search

SQLite full-text search is used for document management search over filenames and extracted text, and may later provide the lexical side of hybrid retrieval. The project avoids adding a separate search service so the single-machine lightweight deployment remains simple.

