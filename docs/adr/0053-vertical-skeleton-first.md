# Vertical Skeleton First

Implementation starts with a thin end-to-end vertical skeleton across Compose, Caddy, backend health, frontend shell, and fake document/model services before deepening individual components. This proves service integration, state ownership, upload flow, indexing flow, and chat flow early, then leaves parser quality, retrieval strategies, UI polish, local models, and evaluation for later iterations.

