# React Vite TypeScript Frontend

The frontend is built with React, Vite, and TypeScript, producing static assets served by the Caddy runtime container. This stack supports upload flows, status views, streaming chat, and admin settings without introducing a server-side frontend runtime into the final deployment.

