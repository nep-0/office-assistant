# Example Env And Local Overrides

The repository provides checked-in example environment configuration and ignores local override files such as `.env`. Compose reads environment values for ports, volume paths, provider defaults, debug mode, and resource limits, while admin-editable active settings are stored in SQLite after first boot.

