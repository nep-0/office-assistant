# Fake Models For Automated Tests

Automated unit and integration tests use deterministic fake chat, embedding, document, and OCR services rather than real cloud or local model calls. Real providers are reserved for manual or scripted smoke and evaluation runs where latency, cost, and nondeterminism are acceptable.

