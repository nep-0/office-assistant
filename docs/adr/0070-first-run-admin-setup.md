# First-Run Admin Setup

When the backend has no users, the UI presents a first-run setup flow to create the initial admin account. After an admin exists, only admins can create or manage users, and the project avoids shipping default credentials.

