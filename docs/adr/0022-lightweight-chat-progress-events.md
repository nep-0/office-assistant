# Lightweight Chat Progress Events

Chat streaming shows lightweight user-facing progress such as knowledge-base search activity and final citations, while hiding raw tool JSON, prompts, embedding scores, and internal agent steps from the main UI. Detailed execution information belongs in logs or an optional developer view rather than the ordinary office workflow.

