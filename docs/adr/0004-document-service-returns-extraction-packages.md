# Document Service Returns Extraction Packages

The document service returns an extraction package rather than precomputed chunks. Chunking, embedding, indexing, and citation span selection stay in the backend so chunking and retrieval remain controlled experiment variables instead of being hidden inside the parser service.

