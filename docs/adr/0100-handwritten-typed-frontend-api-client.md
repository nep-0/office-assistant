# Handwritten Typed Frontend API Client

The frontend initially uses handwritten typed fetch wrappers with centralized error handling rather than generated API clients. OpenAPI and client generation can be added later if the backend API stabilizes and grows enough to justify maintaining a formal specification.

