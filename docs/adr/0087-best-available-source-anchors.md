# Best-Available Source Anchors

The document service returns best-available structural anchors rather than promising uniform coordinate precision across formats. PDFs use page numbers when available, DOCX uses headings or sections unless page data is cheap, PPTX uses slide numbers, XLSX uses sheet names and row or table regions when available, and images use filename/page plus OCR region only when available.

