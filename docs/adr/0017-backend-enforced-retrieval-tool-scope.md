# Backend-Enforced Retrieval Tool Scope

The retrieval tool accepts model-proposed search input, but the backend enforces the selected knowledge base, current user permissions, result limits, citation metadata, and allowed retrieval strategy. The model cannot expand retrieval to arbitrary files, all knowledge bases, or unrestricted result counts through tool arguments alone.

