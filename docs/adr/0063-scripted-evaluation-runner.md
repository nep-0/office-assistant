# Scripted Evaluation Runner

Metrics are collected through a backend or repository evaluation runner rather than manual UI operation. The runner loads the evaluation dataset, runs ingestion, asks prepared questions, captures retrieval, citation, and latency data, and exports results, while the UI remains the presentation demo for the same workflows.

