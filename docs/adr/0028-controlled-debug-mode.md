# Controlled Debug Mode

Debug mode is off by default and can be enabled by an admin setting for development or evaluation, with environment overrides available for test deployments or final private deployments. Debug traces are clearly marked and retention-limited so sensitive prompts, retrieved snippets, and tool details are not silently stored during ordinary use.

