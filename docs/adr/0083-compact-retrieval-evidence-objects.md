# Compact Retrieval Evidence Objects

The retrieval tool returns compact evidence objects rather than raw documents. Each result includes a result ID, bounded chunk content or preview, document title, source anchor, relevance score when available, and citation metadata, with backend-enforced caps on result count and content length.

