# Canonical Extracted Markdown Artifact

Each processed document version stores one canonical extracted Markdown artifact, while chunk records and source-anchor metadata live in SQLite. This keeps extracted content easy to inspect and preview without scattering parser output across many generated files.

