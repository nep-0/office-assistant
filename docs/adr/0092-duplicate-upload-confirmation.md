# Duplicate Upload Confirmation

When upload-time duplicate detection finds matching content in a visible scope, the default action is to cancel the duplicate upload. The user can explicitly choose to upload anyway, and ingestion does not start until that confirmation is given.

