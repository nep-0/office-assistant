# Defer Full Document Rendering

The first version supports original file download and extracted text or Markdown preview, but does not require full in-browser rendering for PDF, DOCX, XLSX, or PPTX. Browser PDF preview may be added if easy, while Office rendering, page-image rendering, and coordinate highlighting remain out of scope.

