# Defer Reranking

Reranking is deferred until vector retrieval and hybrid retrieval are working and measured. A reranker interface may be added if cheap, but no reranker service or model is required for the core demo because it adds latency and another model dependency to the ordinary CPU deployment.

