# Primary Markdown-Normalizing Parser

The document service uses one mature Markdown-normalizing parser path as the primary extraction strategy for supported office inputs, with OCR fallback for scanned or image-based content. Specialized table or media extraction is added only when evaluation shows the primary parser output is insufficient for the final demo and experiments.

