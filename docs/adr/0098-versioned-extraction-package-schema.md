# Versioned Extraction Package Schema

The extraction package includes a schema version so the backend can validate document-service responses and reject incompatible formats clearly. This protects the contract between independently evolving backend and document containers during development.

