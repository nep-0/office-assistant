# Simple Document Management Search

The UI includes simple document management search and filters separate from chat retrieval. Users can find documents by filename, type, status, upload date, and possibly extracted text preview without relying on semantic RAG ranking or chatbot interaction.

