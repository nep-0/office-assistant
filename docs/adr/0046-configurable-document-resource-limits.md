# Configurable Document Resource Limits

The first version enforces conservative, configurable limits for uploads and extraction work, such as maximum file size, page/sheet/slide counts, OCR page counts, and processing timeouts. Oversized or pathological documents fail with clear messages instead of consuming the whole local CPU deployment.

