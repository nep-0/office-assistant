# Separate System and Quality Evaluation

The final report evaluates system behavior separately from answer quality. System metrics include ingestion time, index size, memory use, question latency, and scaling across document counts, while quality metrics include retrieval hit rate, citation correctness, and answer correctness on a prepared question set.

