# Defer Exact Local Model Selection

The project defines local model requirements before naming exact GGUF files. Chat models must be small instruction-tuned models that run on the ordinary CPU deployment target, and embedding models must be compatible with llama.cpp server and provide acceptable retrieval quality; exact tested model names are recorded later in the evaluation report.

