# Document Preview And Download

The UI supports previewing extracted text or Markdown so users can verify ingestion and citations, and allows downloading the original uploaded file. Downloading extracted Markdown is optional or admin/debug-oriented rather than a primary user workflow.

