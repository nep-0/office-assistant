# Privacy-Scoped Duplicate Detection

User-facing duplicate warnings are scoped to the target knowledge base, with optional softer warnings only for duplicate content in knowledge bases the user can access. Global hash metadata may exist internally, but duplicate detection must not reveal private documents owned by other users.

