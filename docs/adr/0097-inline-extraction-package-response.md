# Inline Extraction Package Response

The first version has the document service return the extraction package inline in the HTTP response, including Markdown and metadata, with response size limits. Streaming or artifact-pull protocols are deferred until supported document sizes make inline responses impractical.

